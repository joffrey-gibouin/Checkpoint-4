<div class="col-12 col-sm-9 col-lg-6 text-center">
    <div class="my-5">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-outline-success btn-lg my-5">Ajouter</button>
        {{ form_end(form) }}
    </div>
</div>
